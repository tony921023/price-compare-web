# ── Server ──
SESSION_SECRET=change-me-to-a-random-string
WEB_ORIGIN=http://localhost:5173
PORT=8787
NODE_ENV=development

# ── PostgreSQL (docker-compose + server) ──
POSTGRES_USER=pp
POSTGRES_PASSWORD=change-me
POSTGRES_DB=pricepulse
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Or use a single connection string (overrides individual vars):
# DATABASE_URL=postgresql://pp:change-me@localhost:5432/pricepulse
